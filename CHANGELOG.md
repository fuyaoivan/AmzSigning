# Changelog

版本格式遵循 Semantic Versioning。初始基线为 v1.0.0。

## v1.1.0

- 增加独立项目移除与重新添加，使用管理标识隔离旧扫描和旧续签结果。
- 将手动扫描改为一次性添加操作；已管理项目的续签不再依赖目录范围。
- 统一新项目默认关闭，移除项目名称预设与专用界面逻辑。
- 将应用和运行数据迁移到源码目录；增加双击构建入口，安装后回收构建产物。
- 合并重复扫描入口，仅保留自动模式与离开模式，移除失效范围提示和冗余说明。
- 迁移 v1 状态，保留已有项目、启用状态、设备绑定与续签周期。
- 使用中性应用标识，隔离运行数据并整理公开技术文档。

## v1.0.1

### 修复

- 将项目发现与续签到期判断分离，移除每日发现和窗口启动扫描。
- 使用持久化请求标识处理显式扫描，保留后台忙碌期间及扫描过程中新增的请求。
- 目录范围变化后丢弃过期扫描结果，避免错误更新项目可用状态。
- 兼容旧状态中的每日扫描字段；该字段不再触发扫描，后续写入时移除。
- 将运行资源回收与项目发现分离，无扫描或续签工作时仍执行本地保留策略。
- 使用全新打包目录生成应用；安装时等待任务锁并完整替换应用目录，防止旧资源残留。

### 优化

- 重新设计蓝白配色的矢量图标，统一图标与界面品牌色。
- 移除界面宣传语、重复提示和说明段落，保留状态、时间、错误信息及操作控件。
- 从应用元数据读取界面版本号，移除重复版本字符串。
- 移除旧扫描时钟、重复初始化逻辑及未使用的样式参数。
- 补充扫描请求、并发和旧配置兼容性测试；整理技术文档。

## v1.0.0

- 提供原生 macOS 设置界面和一次性续签任务。
- 支持原生 iPhone 工程识别、Automatic Signing、Personal Team 与已配对设备覆盖安装。
- 实现 6 天续签、6 小时失败重试、暂停、定时恢复和离开模式。
- 实现签名身份校验、原 App 存在检查、进程锁和临时资源回收。
